export default function BubbleSort() {
    return (
        <div className="flex h-full w-full flex-col items-center justify-center">
            <h1 className="text-4xl font-bold">Bubble Sort</h1>
            <p className="mt-4 text-lg">Bubble sort is a simple sorting algorithm that repeatedly steps through the list, compares adjacent elements, and swaps them if they are in the wrong order.</p>
            <p className="mt-2 text-lg">It gets its name from the way smaller elements "bubble" to the top of the list.</p>
        </div>
    );
}